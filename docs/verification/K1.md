# K1 公共 Contracts 验证记录

日期：2026-09-17。基线提交为 `7abaeeeefad92c7554279844be361d4235b26f2d`（V0），作者为 `Dev-Wiki <devwiki2014@gmail.com>`。本轮先按用户要求提交 V0，再实现 K1；K1 保留在工作树，未追加提交、push、tag 或发布。

## 交付与边界

- [公共入口](../../packages/contracts/src/index.ts) 导出请求、结果、验证证据、授权、快照、Run、能力、Packager 与 Registry 接口；TypeBox 0.34.41 / Ajv 8.17.1 由锁文件固定。
- [Schema 目录](../../packages/contracts/schemas/) 保存 21 份同源 JSON Schema；[公共契约 §9](../CONTRACTS.md#9-k1-可执行协议) 说明字段来源、format、关联校验、错误码及环境验证边界。
- [fixtures](../../packages/contracts/fixtures/) 包含 execution / state / packaging 的 21 个正例及 2 个完整非法 JSON；测试另构造版本、身份、授权、路径、时间、验证覆盖和结果边界负例。
- [共享 Executor 工厂](../../tests/contract/executor-contract.mjs) 覆盖 completed / blocked / failed / partial、cancel、invalid / missing result、drift、authorization violation。当前仅由 Fake Executor 运行，不证明任何真实宿主权限、进程树或 Session 隔离。
- Run 布局继续使用 `$(git rev-parse --git-path dev-harness-runtime)/runs/<run-id>/run.json`；没有状态落盘、锁或恢复实现。数据校验不代替 K3 / K3-L / K3-R / K4-V 的真实环境核验。

## 实际验证

工作目录为仓库根，环境为 Node 24.15.0 / pnpm 11.1.0 / Python 3.12 / WSL2。

| 命令 / 检查 | 实际结果 |
|---|---|
| `CI=true pnpm install --no-frozen-lockfile --ignore-scripts` | 依赖安装和锁文件更新通过；非交互重建 node_modules 以匹配当前本地 store |
| `pnpm schemas:write` | 编译后导出 21 份 Schema |
| `pnpm verify` | 通过，退出 0；typecheck / lint / build 成功 |
| Node 单元与集成（上述 verify 内） | 115 项通过，0 失败、0 跳过；含 104 项本轮契约测试与既有 11 项回归 |
| `pnpm schemas:check`（上述 verify 内） | 21 份导出与 TypeBox 源定义逐字节一致 |
| R1 平台 fixtures（上述 verify 内） | 10 项通过，未连接宿主 |
| CLI tarball（上述 verify 内） | 离线安装、dhr bin / help / version 通过 |
| `node --test packages/contracts/tests/execution.test.mjs` | 补充 2 个完整非法 JSON 后重跑相关 23 项，全通过；总测试数保持 115 |

受限沙箱的 Node 子进程捕获限制沿用 V0 环境情况；以上实际测试在已获准的可执行子进程环境运行，没有使用文件级通过摘要替代真实测试结果。未运行原生 Windows / Linux、远端 CI、真实插件安装或模型调用。

审阅补齐了五类声明漏洞并加入回归：验证产物不能包含 Git / Planning / 冻结来源或任务源码路径；非 completed 的 closure 也须绑定当前 Task；协议来源路径不得重复或大小写别名；scope 的代码与 Planning 路径之间拒绝大小写别名；Windows 单反斜杠开头的驱动器相对路径不作为绝对路径接受。

## 计划与范围

K1 的验收正文迁至 [M1 归档](../plan/archive/M1/K1.md)，Dashboard 移除活跃行及工作顺序项，后续依赖改指归档，下一项为 K2。链接、任务唯一性、依赖图、归档完整性和基线漂移由临时检查脚本核验；设计正文、协议锁和两个参考仓库保持原样。

`python3 /tmp/verify_runtime_k1.py` 与 `git diff --check` 通过：48 份 Markdown、353 个本地链接、15 个锚点；18 个活跃任务与执行包一一对应，8 个 ready 任务在工作顺序中各出现一次，依赖无环。12 个既有文件变更和 63 个新增文件均在本任务及收口范围，HEAD 保持 V0 提交，暂存区为空，未发现范围外漂移。
