# R1 平台基线验证记录

日期：2026-09-17。交付为 [平台基线](../integration/PLATFORM_BASELINE.md) 和 [最小样例](../../tests/fixtures/platform-specs/README.md)，任务闭合正文见 [M0 / R1](../plan/archive/M0/R1.md)。

## 提交与取证边界

本任务开始前已按用户要求提交初始化及 R0，完整提交号为 `961e5517734e943960840eed8e5df54c97870d27`，主题 `chore(init): 初始化开发计划与公共运行契约`；作者与提交者均为 `Dev-Wiki <devwiki2014@gmail.com>`，分支 main。R1 开始时工作树干净，结束时成果保留在工作区，未再次提交、push、tag 或发布。

本轮打开官方来源，检查安装包公开声明和 CLI version/help；没有读取凭据或用户会话，没有安装测试插件或启动模型。只读分工取证后复核 DSH 字段摘要与 fixture 入口，Cordis 命名导出 apply 符合静态加载契约；这不构成实际加载证明。

## 可复跑离线检查

在仓库根运行：

```bash
python3 tests/fixtures/platform-specs/check.py
```

结果：10 项测试全部通过，覆盖六类产物的八个 profile、六个 SKILL.md、manifest 必需字段、schema 标识、名称长度边界、缺字段/非法名称负例、缺文件/绝对路径/路径逃逸/symlink 逃逸负例、Skill 目录不匹配、自有无操作模块出口及固定字节摘要。仅为 fixture 子集检查，不是完整官方 schema 或 loader conformance 测试。

另在本机运行随附工具：

```bash
python3 /home/zyz/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py tests/fixtures/platform-specs/codex-compat/plugins/dev-harness-runtime-spec
```

结果：`Plugin validation passed`，退出 0。工具位置是本机取证路径，未复制进产物；两个脚本的 SHA-256 已登记在 sources.json。其他环境可能没有该工具，不能据此把它作为可移植工程依赖。它的严格兼容 profile 与当前官网存在 hooks 等差异，适用范围见平台基线。

## 环境验证

| 检查 | 输出 / 结论 |
|---|---|
| node / pnpm / git / python3 version | v24.15.0 / 11.1.0 / 2.43.0 / 3.12.3 |
| uname | Linux 6.6.87.2-microsoft-standard-WSL2；只验证 WSL2 |
| codex version / exec help | codex-cli 0.154.0，帮助成功；PATH alias 创建有只读 FS 警告 |
| dsh version / 顶层 help | 0.1.5-rc.1；实际依赖组件 rc.2，公开声明摘要已记录 |
| agent version | 2026.06.26-7079533；没有把它当作编辑器版本 |
| cursor version | WSL Vsock socket failed，编辑器验证失败 |
| agy version / help | 1.0.0，有 plugin 子命令；没有插件运行证据 |
| PATH 查找 | opencode / pwsh 未找到；不推断其他 OS 安装情况 |

## 计划收口校验

以开始时 HEAD、已有文件 SHA-256 和干净工作树为基线，对既有文件修改逐一比对允许范围。归档前建立移动映射；R1 正文只保留在 M0，入站链接与正文相对链接一起修正。平台执行包增加基线入口，更新已解决的资料未知项，保留尚未验证的运行条件。

`python3 /tmp/verify_runtime_r1.py` 为本轮临时检查入口，持久结果见下表。检查本地 Markdown 链接与锚点、活跃任务唯一性、ready 顺序、依赖无环、归档闭合、未完成标记和范围外漂移；`git diff --check` 通过。

| 检查项 | 结果 |
|---|---|
| 活跃计划 | 20 个活跃任务；10 个 ready 任务与工作顺序一一对应；首项 V0 |
| 归档 | R0 / R1 正文只在 M0；R1 无遗留活跃链接；依赖无环 |
| 内容与范围 | 41 份 Markdown、314 个本地链接、14 个锚点全部可解析；14 个既有文件修改或移动均在 R1 交付与交接范围；设计来源和参考仓库未变 |
| Git | main，HEAD 为上述初始化提交，暂存区为空 |

## 未取得的证据

未运行 `pnpm verify/build/generate/validate:plugins/pack` 或 `dhr`，这些实现尚不存在。未验证 Windows / 原生 Linux、生产产物、插件安装与卸载、完整依赖锁、独立 Session、取消、恢复或全进程权限强制。R1 登记自动执行前必须证明 authorizationEnforced 的条件，本轮没有自动执行，因此没有把条件登记冒充能力通过。后续任务必须继续满足该门禁。
