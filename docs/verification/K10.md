# K10 统一本地产物当前验证

可信仓库工厂 `createRepositoryBuildPipeline(root, protocolCheckout)` 已一次性注册六平台 Packager，保持 RuntimeAdapter 全部关闭。`pnpm build` 后，`pnpm dhr release --dry-run --protocol-checkout ../dev-harness` 在当前 WSL2 两次运行均通过，六平台产生九个 artifact；两次返回的九个 SHA-256 完全相同。`dist/manifest.json` 含六项 Adapter / 目标格式兼容记录和九项 artifact，独立 Python SHA-256 核对全部一致。实际 `sourceCommit` 为本轮执行时的 `dae556af71d43fdb94b6bb9a2231e48c8376cf48`；后续提交使该本地 manifest 过期，须清理并重建，不能把旧哈希视为新 HEAD 的发布证据。

逐阶段脚本 `pnpm generate`、`pnpm validate:plugins`、`pnpm run pack` 均已用固定本地协议 checkout 执行通过；`pnpm pack` 是 pnpm 自带命令，不能调用本项目脚本。仓库级 `pnpm dhr --version` 返回 0.1.0；独立 CLI tarball 的 `pnpm test:cli-package` 再次通过，保留无默认 Packager / Executor 的能力门禁。`pnpm matrix:write` 从实际 manifest、文件摘要和可信注册表生成 [能力矩阵](../PLATFORM_MATRIX.md)，`pnpm matrix:check` 通过。`pnpm harness:quick` 通过。CI 已配置 Ubuntu / Windows 离线 verify 与 package dry-run 两个 job，但尚未获得远端 runner 运行记录。

K10 仍未完成完整 MVP 验收：Codex / DSH 的逐 Task 授权与真实三 Task Executor 链未通过；Cursor / OpenCode / Antigravity 的完整宿主会话调用尚缺。没有 npm publish、push、tag、GitHub Release、Marketplace 提交或 deploy。正式对外分发所需项目许可仍缺。最终 `pnpm verify` 按用户要求在剩余开发完成后运行。
