# K10-G Portable Agent Plugin 验证

`createAgentPluginBuildPipeline(root, protocolCheckout)` 已从固定协议 checkout、共享 Skill 和元数据生成 `dist/agent-plugin/dev-harness-agent-plugin-v0.1.0.zip`。ZIP 解包后只有 `dev-harness/plugin.json`、三个原样 Skill、README 和真实分发声明六个文件；不包含 Executor、独立 Runtime 或 MCP 服务。manifest 使用 Agent Plugins 1.0.0 字段及本仓 release version，公共静态校验检查严格闭合字段、Skill frontmatter、来源摘要、非法路径与版本漂移。

`pnpm build`、`pnpm harness:quick`、`node --test tests/packaging/*.test.mjs` 均通过；打包专项 50/50、0 跳过。真实来源流水线 `generate → validate → pack` 通过，生成的六文件 ZIP 已独立解包核对。golden 固定树和归档 SHA-256。无可信 Executor 时，`runCli(['run','--adapter','agent-plugin','--task','K1'])` 返回非零并报告 `CAPABILITY_MISSING`；README 明确要求先确认宿主 Adapter。统一 release manifest 记录 `agent-plugin` 的版本、来源提交和产物摘要。

这是平台中立的 Skill 产物，未声称任何特定宿主会话已加载它，也不启用自动 Session 编排。对外分发仍受共享许可门禁约束。实现见 [Portable Packager](../../build/targets/agent-plugin.ts)、[专项测试](../../tests/packaging/agent-plugin.test.mjs) 和 [golden](../../tests/packaging/golden/agent-plugin.json)。最终 `pnpm verify` 按用户要求在所有剩余开发完成后执行。
