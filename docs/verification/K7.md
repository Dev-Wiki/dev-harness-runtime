# K7 Cursor Native Plugin 当前验证

本地生成链已实现：`createCursorBuildPipeline(root, protocolCheckout)` 从锁定 Skill、CLI / Adapter bundle、共享元数据与固定协议 checkout 生成 `.cursor-plugin/plugin.json`，三个 Skill、一个非全局 rule、一个 `dhr-status` command、包内 CLI 和分发声明，打为 `dist/cursor/dev-harness-cursor-v0.1.0.zip`。解包核对 12 个文件；包内 `node scripts/dhr.mjs --version` 返回 0.1.0。封闭 Schema、负例、golden 与公共阶段校验通过。

`pnpm build`、`pnpm harness:quick` 及 `node --test tests/packaging/*.test.mjs` 通过，打包专项 40/40、0 跳过。本任务仍未完成宿主安装 / 发现 / 调用 / 卸载。WSL 内 `cursor --version` 因 Vsock socket 错误不可用；`agent --version` 为 `2026.06.26-7079533` 且 CLI 提供 `--plugin-dir`，但未据此推断 Editor 已加载插件。

针对本地 Agent CLI `--plugin-dir` 的临时只读 smoke 调用被自动审批拒绝：执行可能向外部 Cursor 服务发送私有生成插件和上下文，当前没有该数据边界的明确授权。调用未执行，不能记作宿主通过；未改动用户的 Cursor 插件目录。后续若获得授权，应在临时工作区做原生加载、发现、命令调用及清理，并更新本记录与看板。正式对外发布仍受许可门禁约束。

实现见 [Cursor Packager](../../build/targets/cursor.ts)、[专项测试](../../tests/packaging/cursor.test.mjs) 和 [golden](../../tests/packaging/golden/cursor.json)。最终 `pnpm verify` 按用户要求在所有剩余开发完成后运行。
