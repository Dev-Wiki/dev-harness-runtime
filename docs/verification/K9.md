# K9 Antigravity Plugin 与 Skills 当前验证

`createAntigravityBuildPipeline(root, protocolCheckout)` 已从锁定 Skill、CLI / Adapter bundle、共享元数据和协议 checkout 生成三种 ZIP：Agent Plugin、项目 `.agents/skills/` 与独立 global `skills/`。三个 Skill 变体逐字节一致，Plugin 使用 Agent Plugins 1.0.0 根 manifest，包内 `dhr` 仍由共享 Core 实现；不启用 Antigravity 自动 Executor。

`pnpm build`、`pnpm harness:quick`、`node --test tests/packaging/*.test.mjs` 通过，打包专项 46/46、0 跳过。真实来源流水线的生成、静态验证和三包打包通过。Antigravity CLI `agy plugin validate .generated/antigravity/plugin/plugin` 返回 3 个 Skills processed；在事前 `agy plugin list` 为空的环境下，`agy plugin install` 本地目录成功，`agy plugin list` 显示 `dev-harness` 的 `local-install` 记录，`agy plugin uninstall dev-harness` 后列表重新为空。此安装测试未调用模型，也未证明 Skill 在会话中被调用；宿主 smoke 仍只有安装 / 发现 / 卸载的部分证据。

项目级与 global 独立 Skills 已做 ZIP 路径和 golden 校验，未写入真实项目或用户 global Skills 目录。对外发布仍受共享许可门禁约束。实现见 [Antigravity Packager](../../build/targets/antigravity.ts)、[专项测试](../../tests/packaging/antigravity.test.mjs) 和 [golden](../../tests/packaging/golden/antigravity.json)。最终 `pnpm verify` 按用户要求在剩余开发完成后执行。
