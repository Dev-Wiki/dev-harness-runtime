# K8 OpenCode 双产物当前验证

`createOpenCodeBuildPipeline(root, protocolCheckout)` 已从锁定 Skill、CLI / Adapter bundle、共享元数据和协议 checkout 生成两个独立产物：`dist/opencode/dev-harness-opencode-v0.1.0.tgz` 与 `dist/opencode/dev-harness-opencode-local-v0.1.0.zip`。前者包含 npm 的 `package.json`、命名插件导出、bundle、三个 Skill 和分发声明；后者包含项目 `.opencode/plugins/`、`.opencode/skills/`、runtime 及说明。npm 包内 Skill 不宣称自动发现，需按说明单独放入项目 Skill 目录。两个变体不能同时安装，以免 OpenCode 加载两个本地 / npm 插件。

`pnpm build`、`pnpm harness:quick`、`node --test tests/packaging/*.test.mjs` 通过；打包专项 43/43、0 跳过。真实来源流水线生成、静态验证和打包通过，分别解包核对 npm 10 个文件与本地 9 个文件；两种入口可由 Node 导入。tgz 在临时 npm prefix 下以 `--offline --ignore-scripts` 安装，包内 `dhr --version` 返回 0.1.0；临时目录已删除。

随后将官方 `opencode-ai@1.18.31` 安装于 `/tmp`，使用其 Linux x64 可执行文件（npm 安装禁用 postinstall，未修改全局软件）。在独立临时项目解开当前提交 `a4d7a39d3dc3eabdb53c679ffa8da3d44106a3c9` 的本地 ZIP，以单独的 XDG 目录启动 OpenCode 1.18.31 localhost server。`GET /config` 返回 `file:///tmp/dhr-opencode-smoke/.opencode/plugins/dev-harness.js`，`GET /skill` 返回 `run`、`status`、`worker`，三个 location 均指向该项目的生成 Skill。停止 server、删除该项目 `.opencode/` 后重新启动，`GET /config` 的 plugin 数组为空，`GET /skill` 不再包含这三个名称（总数从 20 降到 17）；已停止第二个 server。宿主初次启动需获取公开模型目录，模型会话没有运行；这些证据证明本地变体被宿主发现及删除后消失，不证明插件 hook 的业务调用或模型在会话内调用 Skill。npm 变体仍只有离线安装和 Node 导入证据。

Task Executor 没有启用。对外 npm 发布还受共享分发许可门禁约束。实现见 [OpenCode Packager](../../build/targets/opencode.ts)、[专项测试](../../tests/packaging/opencode.test.mjs) 和 [golden](../../tests/packaging/golden/opencode.json)。最终 `pnpm verify` 按用户要求在剩余开发完成后执行。
