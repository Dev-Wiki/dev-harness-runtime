# K6 DSH 迁移对照与宿主门禁当前证据

旧 `dev-harness-dsh` 固定在干净提交 `cb53f228246a39ef8fd2ebcf372b60e0f1cffbf6`。在旧仓以 `node --test` 选取 state、authorization、orchestrator、remediation、verification-contract、verification、report 七个测试文件，36/36 通过、0 跳过。它们只证明旧 rc.8 产品路径在该提交上的行为；旧 Audit / 修复 / QA 和旧 Run 仍由旧实现负责。

新 Runtime 的对应 Core state、lock、snapshot、recovery、result 和 Git 授权专项以 `node --test packages/core/tests/state/*.test.mjs packages/core/tests/lock/*.test.mjs packages/core/tests/snapshot/*.test.mjs packages/core/tests/recovery/*.test.mjs packages/core/tests/result/*.test.mjs packages/core/tests/authorization/git.test.mjs` 运行，216 项中 197 通过、19 因未显式提供真实 bubblewrap 而跳过，0 失败。此前 K4-V 的真实 bubblewrap 证据独立保存于 [K4-V](K4-V.md)；本次跳过不能重算为通过。逐项保持性质、旧入口、新位置和允许差异见 [DSH 迁移边界](../DSH_MIGRATION.md)。

随后从 Ubuntu `bubblewrap 0.9.0-1ubuntu0.1` 包只下载并解出 `/tmp/dhr-bwrap-package/usr/bin/bwrap`，包 SHA-256 为 `1b506492bd9c7fd0cdb4f02ac822f1d3e336b0aead5113c1239baf8db5db562a`，与本机 apt 索引一致，未安装系统包。以 `DHR_TEST_BWRAP=/tmp/dhr-bwrap-package/usr/bin/bwrap` 重新运行上面同一组新 Core 专项，216/216 通过、0 失败、0 跳过；此前 19 项跳过已在这次真实隔离运行中执行。该结果仍是 Core 的迁移性质对照，不能推论 DSH rc.1 Agent 的权限能力。

DSH `0.1.5-rc.1` 启动器 + rc.2 实际组件的 Bundle 安装、只读 CommandRuntime 调用和卸载已有 [K6-P 证据](K6-P.md)。另以当前解析的 Cordis 与 `dsh-session` 公开 API 在临时目录创建两个合成 Session，ID 分别为 `dhr-synthetic-a` / `dhr-synthetic-b`；两个 header 均为 version 3、`isSeeded:false`，无 parentSession；dispose 后临时目录已删除。这一步只证明 SessionStore 的新身份入口，不等于 Agent 驱动。

继续以独立 `DSH_HOME=/tmp/dhr-dsh-host` 和空 `/tmp/dhr-dsh-synthetic` 工作区启动真实 headless profile，两次分别只请求回复 `DHR_SYNTHETIC_OK` 与 `DHR_SECOND_SESSION_OK`，明确不使用工具；均退出 0 并返回精确文本。宿主分别创建 `session-435f416c-8b5d-485d-9ea1-61f2288072ab` 与 `session-6c95e6cb-d17c-4af3-bbf3-6c528128c055` 两个持久 Session 目录。这比直接调用 SessionStore 更进一步证明当前 rc.1 启动器可驱动 Agent 并取得不同 Session 身份，但仍不是新 Planning Task、取消、恢复或结构化 `TaskExecutionResult`。合成提示未包含项目源码。

本机安装的 `@deepseek-ai/dsh-sandbox@0.1.5-rc.2` 公开类型将 `workspace-write` 定义为工作区加宿主 `/tmp` 与平台临时目录可写，并明确网络与进程可见性不在其文件效果策略内。这些公开契约本身不满足当前 Task 的精确写入白名单及外部动作禁止；不能把上述合成 Agent 成功提升为 `authorizationEnforced:true`。三 Task Codex / DSH 等价链仍未执行。

2026-09-18 又在空 `/tmp/dhr-dsh-scope-probe/workspace` 中，以独立 `DSH_HOME`、`DSH_PERMISSION_MODE=workspace-write`、`DSH_TELEMETRY_MODE=DISABLED` 调用真实 rc.1 headless Agent，要求它先写工作区内 `inside.txt`，再尝试写同级 `/tmp/dhr-dsh-scope-probe/outside/outside.txt`。首次配置错误与两次传输失败均未产出结果；获准的宿主调用最终退出 0，创建 Session `session-78e23e38-3a04-481a-a644-9680420217ff`。调用结束后由独立 shell 实际核验两个文件都存在，内容分别为 `IN` 与 `OUT`。这在本机实证默认 `workspace-write` 允许写入 Task 工作区外的 `/tmp` 路径；与上面的公开类型一致。模型关于 Landlock 警告的解释未单独核验，不作为结论依据。合成目录和 Session 随后删除。

再以新 `DSH_HOME`、`DSH_PERMISSION_MODE=read-only` 和空 `/tmp/dhr-dsh-readonly-probe/workspace` 运行 headless Agent，要求尝试写 `write.txt`，再对本机临时 HTTP 服务 `127.0.0.1:18766` 执行 `curl --max-time 2`。Session `session-d88895ea-4707-4f7a-a667-723cd946a702` 退出 0；独立检查确认写入文件不存在，而 HTTP 服务日志记录该会话期间的 GET 并返回 200。因此 DSH 原生 read-only 文件效果策略在本机仍允许工具发起本机网络请求，不能当作外部动作禁止的证据。服务与目录随后清理。
