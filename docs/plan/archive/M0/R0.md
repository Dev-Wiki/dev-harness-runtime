# 任务 R0：补齐公共契约与迁移边界

> 本文只维护该任务的实施与验收详情。状态、优先级、依赖、执行顺序和阻塞以 [Dashboard](../../Dashboard.md) 为唯一权威来源。

## 最终交付

完成公共契约、设计决策和 DSH 行为迁移清单；公共实施任务据此更新执行包。完成的是设计任务，不代表 Core / Adapter 已实现。

## 背景与目标

将设计中的接口示例和行为原则收敛成可供独立实现的决策记录，明确仍需项目所有者决定的产品边界。

## 执行上下文

- **权威需求**：[设计文档](../../../../../dev-harness-runtime-design.md) §5–17、§22、§26–28、§37–38；[资料评估](../../Readiness.md)。
- **代码入口**：从下列影响文件进入。初始化时目标项目为空，所列实现与测试路径均为建议新建路径，不能当作已有代码。
- **相关测试**：逐条走查成功、失败、崩溃和恢复的事件序列；核对协议字段和旧 DSH 测试映射。
- **必须保持的不变量**：上游协议负责 Planning / Git 语义；Runtime 只执行和校验；项目名称、CLI 和状态根目录沿用用户已确认决定。
- 参考 [Planning 协议](../../../../../dev-harness/planning/SKILL.md)、[旧 DSH 架构](../../../../../dev-harness-dsh/ARCHITECTURE.md)、[旧 DSH 状态实现](../../../../../dev-harness-dsh/src/state.ts)。

## 范围

- **包含**：逐项处理资料评估 G1–G5、G7；形成 Planning 输入、收口责任、授权、状态恢复、来源锁定与 DSH 迁移范围的决策记录。
- **不包含**：实现 Runtime、改写上游 Planning 协议、运行真实付费宿主或修改旧 DSH 仓库。

## 影响文件

以下路径以 `dev-harness-runtime/` 为根；执行前核对已建立的目录与命令，已有文件按职责更新。

- `docs/decisions/runtime-contracts.md`
- `docs/CONTRACTS.md`
- `docs/DSH_MIGRATION.md`
- `docs/verification/R0.md` — 验收走查与静态文档验证记录。
- 本任务归档、Dashboard、文档导航和受影响执行包 — 交接与生命周期收口。

## 建议实施顺序

1. 重读 Dashboard 与本任务，检查来源是否漂移；从资料评估对应条目和已形成的决策记录确认输入。
2. 先建立本任务验收所列的正常、异常和边界样例，再在范围内实现或完成决策取证；不为迁就实现修改公共协议。
3. 按下列验收逐项验证，记录真实命令、环境、结果和稳定证据；更新相关事实文档，再按 Planning 生命周期收口。

## 验收标准

- [x] G1–G5、G7 每一问题都有明确决定、理由和来源；不能决定的产品问题形成可回答的问题，不默认为已解决。
- [x] 定义 Dashboard 可接受语法、依赖完成的归档证据、blocker 解除表达和显式选取任务的资格规则。
- [x] 明确 Worker 与 Core 谁写计划、归档、执行验证和提交，以及多文件收口在每个崩溃点的恢复规则。
- [x] DSH 通用行为、新旧产品流程、旧 Run 兼容性逐项分类；未承诺迁移 Audit / QA 全流程。

## 验证证据

| 验证项 | 命令 / 操作 | 结果 / 证据链接 |
|---|---|---|
| 本任务验收 | 公共契约事件走查、独立只读审阅和旧源码 / 测试名称映射 | 通过；[R0 验证记录](../../../verification/R0.md)。三份交付文档已形成，14 条旧测试名称核对存在。 |
| 文档收口验证 | Python 标准库检查 Markdown 本地链接 / 锚点、Task ID、依赖、字段归属与文件指纹 | 通过；见 [验证记录](../../../verification/R0.md)。本次不含 Runtime 实现测试。 |

## 已确认决策

- 公共实施规则见 [CONTRACTS](../../../CONTRACTS.md) 与 [决策 D01–D14](../../../decisions/runtime-contracts.md)；迁移范围见 [DSH_MIGRATION](../../../DSH_MIGRATION.md)。
- 首版只迁移通用机制；旧 Audit / 修复 / QA 留在旧仓；旧 Run 由旧实现读取，新 Runtime 不自动转换。
- 项目名 `dev-harness-runtime`，CLI 全部使用 `dhr`。
- 状态根目录统一为 `$(git rev-parse --git-path dev-harness-runtime)/runs/`；具体布局以设计 §10 为准。
- 上游协议负责 Planning / Git 语义；Runtime 只执行和校验；项目名称、CLI 和状态根目录沿用用户已确认决定。

## 未知项与停止条件

- **已知风险**：来源升级、接口不完整或环境不足可能使验收不可复现；不能将设计示例或历史通过记录当作本次运行证据。
- **未决问题**：本任务的迁移范围和旧 Run 策略已由用户确认。工程落地、分发许可文件与平台实测按已定义的后续门禁处理，不作为本任务的未决设计问题。
- **停止条件**：需要扩大范围、改变公共语义或验收、使用无证据宿主能力，或发现 Git / Planning 外部漂移时停止实现，回到 Dashboard 对齐。

---

*验收日期：2026-09-17（R0 文档交付与走查完成；未执行 Runtime 或宿主测试）*
