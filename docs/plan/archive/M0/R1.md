# 任务 R1：建立平台格式与验证环境基线

> 本文只维护该任务的实施与验收详情。状态、优先级、依赖、执行顺序和阻塞以 [Dashboard](../../Dashboard.md) 为唯一权威来源。

## 背景与目标

为每个平台建立带来源、日期、目标版本和证据等级的格式基线，给后续 Packager 与 Executor 提供可验证输入。

## 执行上下文

公共能力和授权要求见 [CONTRACTS §3–4](../../../CONTRACTS.md#3-执行请求结果和能力)。

- **权威需求**：[设计文档](../../../design/runtime-design.md) §14、§19–21、§29、§33、§49；[资料评估](../../Readiness.md)。
- **代码入口**：从下列影响文件进入。初始化时目标项目为空，所列实现与测试路径均为建议新建路径，不能当作已有代码。
- **相关测试**：打开官方规范并记录稳定版本或校验摘要；离线校验最小 manifest 样例；只读盘点宿主版本和测试条件。
- **必须保持的不变量**：插件格式与执行能力分别取证；缺少独立 Session 证据时 freshSession 不能为 true。
- 已有官网初查见 [资料评估](../../Readiness.md)；DSH 目标按设计 §19.2 为 `0.1.5-rc.1`，[旧 rc.8 集成基线](../../../../../dev-harness-dsh/docs/integration/DSH_API_BASELINE.md) 仅作历史对照。

## 范围

- **包含**：五个平台及 Portable Agent Plugin 的官方格式、必需字段、入口、能力探测方式；宿主、操作系统和 smoke 条件清单。
- **不包含**：把官网说明当作运行成功；隐式启用模型调用、安装用户级插件或发布产物。

## 影响文件

以下路径以 `dev-harness-runtime/` 为根；执行前核对已建立的目录与命令，已有文件按职责更新。

- `docs/integration/PLATFORM_BASELINE.md`
- `tests/fixtures/platform-specs/`

## 建议实施顺序

1. 重读 Dashboard 与本任务，检查来源是否漂移；从资料评估对应条目和已形成的决策记录确认输入。
2. 先建立本任务验收所列的正常、异常和边界样例，再在范围内实现或完成决策取证；不为迁就实现修改公共协议。
3. 按下列验收逐项验证，记录真实命令、环境、结果和稳定证据；更新相关事实文档，再按 Planning 生命周期收口。

## 验收标准

- [x] 六类产物均有规范出处、最小 fixture、必需字段清单；无法获取的规范有明确缺项。
- [x] 记录 Codex / DSH 可用测试入口；区分文档支持、离线验证、真实宿主运行三种证据。
- [x] 固定 DSH `0.1.5-rc.1` 的公开 API / Bundle 规范、实际组件依赖和版本输出证据；记录相对 rc.8 的兼容差异，不把宿主版本机械套用到所有组件包。
- [x] Windows / Linux / WSL 的可执行测试环境与无法运行的项目均被记录。
- [x] 任何尚无规范或运行证据的能力在后续任务中保持门禁，不因 R1 文档完成自动启用。

- [x] 已登记条件门禁：自动执行前取得 authorizationEnforced 证据；Worker、Core 验证进程与提交 hook 均受授权约束，不能只靠 prompt 禁令。本任务未自动执行，该运行条件尚未满足，后续不得绕过。

## 验证证据

交付 [平台基线](../../../integration/PLATFORM_BASELINE.md)、[最小 fixtures](../../../../tests/fixtures/platform-specs/README.md) 与 [R1 验证记录](../../../verification/R1.md)。

| 验证项 | 命令 / 操作 | 结果 |
|---|---|---|
| 六类格式 | 打开官方规范，读取安装包公开声明，建立八个 fixture profile | 来源、必需字段、格式边界和缺项均有记录 |
| 离线校验 | `python3 tests/fixtures/platform-specs/check.py` | 10 项通过，包括缺字段、非法名称、版本与路径逃逸负例 |
| Codex 兼容格式 | 本机随附 `validate_plugin.py` | 通过；工具摘要已登记，仅适用于本 fixture profile |
| 本机入口 | version/help 和 PATH 只读盘点 | Codex 0.154.0，DSH rc.1 + rc.2 组件，agent 2026.06.26-7079533，agy 1.0.0；Cursor 编辑器入口失败 |
| 宿主与 OS | WSL2 记录、原生 OS 和各宿主 smoke 缺口登记 | 未安装插件或调用模型；没有 Executor 能力通过结论 |
| 收口 | 文档链接、任务唯一性、工作顺序、依赖、归档和范围外漂移 | 见验证记录；不替代 Runtime 测试 |

## 已确认决策

- 项目名 `dev-harness-runtime`，CLI 全部使用 `dhr`。
- DSH 目标宿主固定为 `0.1.5-rc.1`；不直接继承 rc.8 的兼容性结论。
- 状态根目录统一为 `$(git rev-parse --git-path dev-harness-runtime)/runs/`；具体布局以设计 §10 为准。
- 插件格式与执行能力分别取证；缺少独立 Session 证据时 freshSession 不能为 true。

## 未知项与停止条件

- **已知风险**：来源升级、接口不完整或环境不足可能使验收不可复现；不能将设计示例或历史通过记录当作本次运行证据。
- **剩余验证**：公开格式与当前环境已取证；完整依赖可复现性、生产 validator、真实安装、独立 Session、取消、恢复、全进程授权与原生 OS 证据由后续平台任务取得。
- **停止条件**：需要扩大范围、改变公共语义或验收、使用无证据宿主能力，或发现 Git / Planning 外部漂移时停止实现，回到 Dashboard 对齐。

---

*最后更新：2026-09-17（六类格式、最小 fixture 与环境基线验收归档）*
