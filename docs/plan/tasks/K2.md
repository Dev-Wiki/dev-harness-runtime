# 任务 K2：发现项目并读取、选择 Planning Task

> 本文只维护该任务的实施与验收详情。状态、优先级、依赖、执行顺序和阻塞以 [Dashboard](../Dashboard.md) 为唯一权威来源。

## 背景与目标

只从权威 Dashboard 读取工作顺序、任务路径与执行资格，确定唯一下一任务。

## 执行上下文

- **权威需求**：[设计文档](../../design/runtime-design.md) §7、§45 K2；[资料评估](../Readiness.md)。
- **公共实施依据**：[CONTRACTS](../../CONTRACTS.md)、[R0 决策](../../decisions/runtime-contracts.md)、[DSH 迁移边界](../../DSH_MIGRATION.md)。
- **代码入口**：从下列影响文件进入。初始化时目标项目为空，所列实现与测试路径均为建议新建路径，不能当作已有代码。
- **相关测试**：临时 Git 仓库和 Markdown fixtures；根目录冲突、归档依赖、路径歧义、重复 ID 与选取模式矩阵。
- **必须保持的不变量**：Dashboard 是唯一活跃索引；选择有歧义时拒绝执行；不扫描全部归档正文。
- 权威语义见 [Planning Skill](../../../../dev-harness/planning/SKILL.md)；不读取旧 DSH TaskDetails 作为新协议。

## 范围

- **包含**：Git / linked worktree、doc/docs、Project Contract、Dashboard 与任务路径解析，explicit / next / all-ready 的选择逻辑。
- **不包含**：维护第二份计划状态；按优先级重新排序；运行时创建竞争文档根。

## 影响文件

以下路径以 `dev-harness-runtime/` 为根；执行前核对已建立的目录与命令，已有文件按职责更新。

- `packages/core/src/discovery/`
- `packages/core/src/planning/`
- `packages/core/tests/planning/`
- `tests/fixtures/planning/`

## 建议实施顺序

1. 重读 Dashboard 与本任务，检查来源是否漂移；从资料评估对应条目和已形成的决策记录确认输入。
2. 先建立本任务验收所列的正常、异常和边界样例，再在范围内实现或完成决策取证；不为迁就实现修改公共协议。
3. 按下列验收逐项验证，记录真实命令、环境、结果和稳定证据；更新相关事实文档，再按 Planning 生命周期收口。

## 验收标准

- [ ] 主仓和 linked worktree 均解析正确；doc/docs ownership 不明确时失败。
- [ ] 只按权威工作顺序选择满足资格的任务；不存在任务和未完成依赖给出可诊断错误。
- [ ] 归档完成依赖、重复任务、缺少任务文件、路径越界及非规范 blocker 均有测试。

## 验证证据

| 验证项 | 命令 / 操作 | 结果 / 证据链接 |
|---|---|---|
| 本任务验收 | 临时 Git 仓库和 Markdown fixtures；根目录冲突、归档依赖、路径歧义、重复 ID 与选取模式矩阵。 | 尚未执行；交付时记录真实结果与稳定证据。 |
| 共享回归 | 采用 Dashboard 的共享验证基线与届时 HARNESS 已验证入口 | 尚未执行；当前无 Runtime 实现或命令通过记录。 |

## 已确认决策

- 项目名 `dev-harness-runtime`，CLI 全部使用 `dhr`。
- 状态根目录统一为 `$(git rev-parse --git-path dev-harness-runtime)/runs/`；具体布局以设计 §10 为准。
- Dashboard 是唯一活跃索引；选择有歧义时拒绝执行；不扫描全部归档正文。

## 未知项与停止条件

- **已知风险**：来源升级、接口不完整或环境不足可能使验收不可复现；不能将设计示例或历史通过记录当作本次运行证据。
- **未决问题**：Markdown 子集、归档依赖、blocker、显式资格和队列终止已在 [CONTRACTS §2](../../CONTRACTS.md#2-planning-读取协议) 定义；不再按自然语言猜测。
- **停止条件**：需要扩大范围、改变公共语义或验收、使用无证据宿主能力，或发现 Git / Planning 外部漂移时停止实现，回到 Dashboard 对齐。

---

*最后更新：2026-09-17（接入 R0 已确定契约，尚未开始实现）*
